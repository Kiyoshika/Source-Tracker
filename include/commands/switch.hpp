#ifndef SWITCH_HPP
#define SWITCH_HPP

#include <string>
#include <filesystem>
#include <iostream>

namespace commands
{
    void switch_branch(std::string branch_name);
}; // namespace commands
#endif